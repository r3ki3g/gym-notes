// Single source of truth for the app version. Bump here, nowhere else.
//
// Semantic versioning, read as:
//   MAJOR — a change that breaks stored data or needs a migration
//   MINOR — a new feature, backwards compatible
//   PATCH — a fix, no new behaviour
//
// Keep CHANGELOG.md in step, and tag the commit: git tag v<version>
export const VERSION = '1.9.0';
